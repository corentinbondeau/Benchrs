# Fragment — lib-misc
> Generated by @init_project on 2026-08-18
> Module path: src/lib/ (miscellaneous files)

## Key Files
| File | Role |
|------|------|
| players.ts | `fetchTeamActivePlayers(teamId)` — used across many pages |
| chat.ts | Chat helpers: `shouldShowChannel`, `ensureChatMemberships`, `fetchChannelRecipients` |
| clubs.ts | Club helpers: `normalizeFffNumber`, `normalizeClubName` |
| goals.ts | Goal helpers: `GOAL_CATEGORIES`, `currentSeasonLabel`, `seasonDateRange` |
| push.ts | Client-side push notification registration |
| useChatUnread.ts | Hook: realtime unread count per channel (3 Supabase listeners) |
| useSelectedChild.ts | Hook: multi-child selection for parents |
| usePushNotifications.ts | Hook: push notification permission + registration |
| convocations.ts | `ensureAttendanceRows` — creates attendance on notification send |
| playerAlerts.ts | VMA/VMI alerts, departure notifications |
| storage.ts | Storage helpers |
| utils.ts | `cn()` utility (clsx + tailwind-merge) |
| positions.ts | POSITIONS constant for player positions |
| vmaNorms.ts | VMA/VMI reference norms |
| notificationTypes.ts | Notification type constants |
| rateLimit.ts | API rate limiting |
| tabs.ts | Tab persistence helpers |

## Performance Notes
- `useChatUnread` maintains 3 realtime listeners permanently — even when not on chat page
- `usePushNotifications` runs in background on every dashboard page
- `fetchTeamActivePlayers` is called from many pages — good candidate for caching
