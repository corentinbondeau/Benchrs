# Fragment — app-auth
> Generated by @init_project on 2026-08-18
> Module path: src/app/(auth)/

## Responsibility
Authentication pages: login, register (3 modes: player/coach/committee), join team, forgot/reset password, create team.

## Key Files
| File | Role |
|------|------|
| layout.tsx | Auth layout (no sidebar/nav) |
| login/page.tsx | Email/password login |
| register/page.tsx | Registration with 3 modes + FFF club lookup |
| join/page.tsx | Join team via invite code |
| create-team/page.tsx | Create team + club |
| forgot-password/page.tsx | Password reset request |
| reset-password/page.tsx | Password reset form |

## Patterns
- All `"use client"` — form handling with useState
- Direct Supabase auth calls: `supabase.auth.signInWithPassword`, `supabase.auth.signUp`
- After join/register: `localStorage.selectedTeamId` set + redirect to `/link-child?teamId=` (parents) or `/` (others)

## Performance Notes
- Auth pages are lightweight, not a performance concern
- `<img src="/favicon.png">` used instead of `next/image` (minor)
