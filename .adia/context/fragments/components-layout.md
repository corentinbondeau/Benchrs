# Fragment — components-layout
> Generated by @init_project on 2026-08-18
> Module path: src/components/layout/

## Responsibility
App shell: navigation (sidebar for desktop, bottom nav for mobile), top bar with team switcher.

## Key Files
| File | Role |
|------|------|
| Sidebar.tsx | Desktop sidebar (372 LOC) — nav links, team switcher, club nav |
| BottomNav.tsx | Mobile bottom nav (441 LOC) — nav links, chat unread badge, team create |
| TopBar.tsx | Header bar (~100 LOC) — logo, team name, team logo |
| MobileNav.tsx | Mobile navigation variant (deprecated?) |

## Patterns
- Navigation items filtered by role (coach/player/parent) and club memberships
- Chat unread badge via `useChatUnread().total`
- Team colors applied via CSS variables (`--color-gold`, `--color-navy`)
- `<img>` tags for logos (not `next/image`)

## Performance Notes
- Sidebar and BottomNav are always rendered (even if one is `hidden` via CSS)
- `useChatUnread` maintains a realtime subscription (always active)
- Both components import many Lucide icons — tree-shaking should handle this
