# Fragment — app-api
> Generated by @init_project on 2026-08-18
> Module path: src/app/api/

## Responsibility
50+ Next.js API Route Handlers: authentication, notifications (push + cron), AI generation (Mistral), exports (PDF, CSV, ICS), CRUD operations, team management.

## Key Files
| File | Role |
|------|------|
| notifications/cron/route.ts | 824 LOC — Vercel cron: scheduled notifs + eve reminders |
| notifications/send/route.ts | 236 LOC — send push + in-app notifications |
| trainings/generate/route.ts | AI session generation via Mistral |
| matches/report/route.ts | AI/manual match reports |
| championships/dofa/route.ts | FFF DOFA API proxy |
| auth/create-team/route.ts | Team + club creation |

## Patterns
- Every route starts with `getAuthUser(request)` for auth
- Team scoping via `isTeamMember(userId, teamId)` or `isTeamCoach(userId, teamId)`
- AI routes: `fetch("https://api.mistral.ai/v1/chat/completions", ...)` — direct, no SDK
- PDF routes: `@react-pdf/renderer` (server-only, no client bundle impact)
- Admin operations: `createAdminClient()` for bypassing RLS

## Performance Notes
- API routes are serverless (Vercel) — cold start may add latency
- Cron route (824 LOC) is the most complex — processes all teams' reminders
- No API caching headers set (no `Cache-Control`, no `stale-while-revalidate`)
