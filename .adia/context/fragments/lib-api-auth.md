# Fragment — lib-api-auth
> Generated by @init_project on 2026-08-18
> Module path: src/lib/api-auth.ts

## Responsibility
Server-side API auth helpers used by all Route Handlers.

## Exports
- `getAuthUser(request)` — validates Bearer token via admin client
- `unauthorized()` / `forbidden()` — standard error responses
- `getUserTeamIds(userId)` — list user's team IDs
- `isTeamMember(userId, teamId)` — check membership
- `getTeamRole(userId, teamId)` — get role in team
- `isTeamCoach(userId, teamId)` — check coach/owner role

## Notes
- Uses `createAdminClient().auth.getUser(token)` — requires valid `SUPABASE_SERVICE_ROLE_KEY`
- Server-only, no client bundle impact
