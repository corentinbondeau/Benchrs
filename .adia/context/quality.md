# Quality
> Generated by @init_project on 2026-08-18 — Project benchrs
> Last update: 2026-08-18

## Linting
- **ESLint**: `eslint.config.mjs` — `core-web-vitals` + `typescript` rules
- **Command**: `npm run lint` / `eslint`

## Formatting
- No Prettier configured
- No `.editorconfig`
- Indentation: 2 spaces (de facto standard from codebase)

## Pre-commit Hooks
- None (no `.pre-commit-config.yaml`, no `.husky/`)

## Static Analysis
- TypeScript strict mode enabled
- No SonarQube

## Code Coverage
- Not configured

## Security Headers
- CSP, HSTS, X-Frame-Options DENY, nosniff, Referrer-Policy (in `next.config.ts`)
- API routes secured via `getAuthUser` + team scoping
- SSRF protection on `/api/championships/fff` (blocks `*.fff.fr`)
