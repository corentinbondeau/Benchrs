# CI/CD
> Generated by @init_project on 2026-08-18 — Project benchrs
> Last update: 2026-08-18

## GitHub Actions (`.github/workflows/ci.yml`)
- **Triggers**: push to `main`/`production`, PRs
- **Jobs**:
  1. `lint-and-build`: Typecheck (`tsc --noEmit`) → Lint (`eslint`) → Build (`next build`)
  2. `e2e`: Playwright tests (Chromium only, timeout 30min)
- **Node**: 22 (CI) vs 20.9+ (local engines)
- **Secrets**: `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `MISTRAL_API_KEY`

## Deployment — Vercel
- **Preview**: any push to `main` or PR → Vercel preview deployment
- **Production**: merge to `production` branch → Vercel prod deployment
- **Rule**: ALWAYS validate preview before prod (`git push origin main:production`)
- **Build command**: `next build` (via `vercel-build` script)

## Cron Jobs (`vercel.json`)
| Path | Schedule | Role |
|------|----------|------|
| `/api/sporteasy/ical-cron` | 06:00 daily | Sync SportEasy calendar |
| `/api/notifications/cron` | 20:00 daily | Send scheduled notifications + eve reminders |
| `/api/motm/open` | 22:30 Sunday | Open Man of the Match voting |

## Docker
- None (Vercel serverless)

## Database Migrations
- Manual SQL execution in Supabase SQL Editor
- Files in `supabase/migrations/` (74+ files)
- No automated migration runner
