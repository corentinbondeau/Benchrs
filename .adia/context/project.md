# Project — Benchrs (SportPlus)
> Generated by @init_project on 2026-08-18 — Project benchrs
> Last update: 2026-08-18

## Identity
- **Name**: Benchrs (SportPlus)
- **Repo**: benchrs
- **Type**: Full-stack SPA (PWA) — mobile-first team management app for amateur football
- **Languages**: TypeScript (100%)
- **Framework**: Next.js 16 (App Router) + React 19
- **UI**: Tailwind CSS 4 + shadcn/ui (base-nova style) + Lucide icons
- **Database**: Supabase (PostgreSQL + Realtime + Storage + RLS)
- **Auth**: Supabase Auth (JWT) via `@supabase/ssr`
- **AI**: Mistral API (fetch direct, no SDK) — training sessions, match reports, announcements, challenges
- **Deploy**: Vercel (crons, preview → production branch workflow)
- **Version**: 0.1.0

## Key Numbers
- **Pages**: 54 (53 client-side, 1 server component)
- **API Routes**: 50+
- **Components**: 80+ (largest: PlayerProfile 1175 LOC, LiveMatchTracker 1087 LOC)
- **Types**: 923 lines in single `src/types/index.ts`
- **SQL Migrations**: 74+
- **Total .ts/.tsx files**: ~238

## Critical Architecture Note
- **Almost no Server Components**: 99% of pages are `"use client"` — data is fetched client-side via Supabase SDK in useEffect hooks
- **No SSR data fetching**: No `fetch()` in server components, no `getServerSideProps` equivalent
- **Custom query cache**: `useQueryCache` (30s TTL, in-memory Map) replaces React Query / SWR
- **Dashboard waterfall**: Each widget independently fetches from Supabase on mount
- **Heavy client bundle**: recharts, @react-pdf/renderer, html-to-image, @dnd-kit all imported client-side
