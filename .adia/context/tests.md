# Tests
> Generated by @init_project on 2026-08-18 — Project benchrs
> Last update: 2026-08-18

## Framework
- **E2E**: Playwright (`@playwright/test` ^1.62.1)
- **Unit/Integration**: None configured

## Test Structure
- `e2e/public.spec.ts` — single E2E test file
- `playwright.config.ts` — Chromium desktop + Pixel 5 mobile
- Web server: `npm run build && npm run start` on port 3000

## Commands
- `npx playwright test` — run E2E tests
- `npx playwright test --project=chromium` — desktop only
- `npx playwright test --project=mobile-chromium` — mobile only

## Coverage
- No coverage tool configured
- No unit test framework (no Jest, Vitest, or equivalent)

## CI Integration
- Playwright runs in GitHub Actions (`e2e` job)
- Uploads `playwright-report/` on failure

## Gaps
- No unit tests for hooks, lib functions, or API routes
- No component tests
- No integration tests for Supabase queries
- No performance benchmarks or Lighthouse CI
