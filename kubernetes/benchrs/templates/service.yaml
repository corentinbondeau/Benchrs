apiVersion: v1
kind: Service
metadata:
  name: {{ include "Benchrs.fullname" . }}
  labels:
    {{- include "Benchrs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Benchrs.selectorLabels" . | nindent 4 }}
