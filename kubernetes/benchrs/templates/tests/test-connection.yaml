apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Benchrs.fullname" . }}-test-connection"
  labels:
    {{- include "Benchrs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Benchrs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
