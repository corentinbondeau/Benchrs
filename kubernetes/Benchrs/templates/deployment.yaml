apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "Benchrs.fullname" . }}
  labels:
    {{- include "Benchrs.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "Benchrs.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "Benchrs.selectorLabels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "Benchrs.serviceAccountName" . }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: http
          readinessProbe:
            httpGet:
              path: /
              port: http
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
