// Ré-export depuis la source de vérité unique (src/lib/lineup/positions.ts).
// Signature publique et ordre des libellés inchangés : ne rien casser côté
// settings/page.tsx, qui consomme cette liste pour les valeurs persistées en base
// (profiles.position / profiles.secondary_positions).
export { POSITIONS } from "./lineup/positions";
